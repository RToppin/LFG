services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: lfg
      POSTGRES_PASSWORD: lfg
      POSTGRES_DB: lfg
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
